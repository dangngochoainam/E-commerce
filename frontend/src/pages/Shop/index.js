import React, { useEffect, useState } from "react";
import { Outlet } from "react-router-dom";


const Shop = () => {

  return (
    <section>
      <Outlet />
    </section>
  );
};

export default Shop;
