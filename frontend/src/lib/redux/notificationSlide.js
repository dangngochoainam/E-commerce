import { createSlice } from '@reduxjs/toolkit';

const notificationSlide = createSlice({
  name: 'notification',
  initialState: [],
  reducers: {
    
  },
});

export const {} = notificationSlide.actions;

export default notificationSlide.reducer;
